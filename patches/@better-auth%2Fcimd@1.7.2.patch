diff --git a/dist/node.mjs b/dist/node.mjs
index a12d50e19d1fa726246f9a2fbe6f76d07471c0ee..52e2d9e2ea627854002cb2ec5b7e11ef992b215e 100644
--- a/dist/node.mjs
+++ b/dist/node.mjs
@@ -38 +38 @@ const fetchClientMetadataResource = async (input, init) => {
-	const pinnedAddress = addresses[0];
+	const pinnedAddress = addresses.find(({ family }) => family === 4) ?? addresses[0];
@@ -49,2 +49,3 @@ const fetchClientMetadataResource = async (input, init) => {
-			lookup: (_hostname, _options, callback) => {
-				callback(null, pinnedAddress.address, pinnedAddress.family);
+			lookup: (_hostname, options, callback) => {
+				if (options.all) callback(null, [pinnedAddress]);
+				else callback(null, pinnedAddress.address, pinnedAddress.family);
